info:
  name: get-session
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}auth/get-session"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
