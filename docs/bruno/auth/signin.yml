info:
  name: signin
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}auth/sign-in/email"
  body:
    type: json
    data: |-
      {
        "email": "shanelka@mail.com",
        "password": "12341234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
