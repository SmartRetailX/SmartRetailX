info:
  name: revoke-sessions
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}auth/revoke-sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
