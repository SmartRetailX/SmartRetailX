info:
  name: list-session
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}auth/list-sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
