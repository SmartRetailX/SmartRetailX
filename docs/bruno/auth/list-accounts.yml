info:
  name: list-accounts
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}auth/list-accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
