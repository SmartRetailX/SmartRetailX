info:
  name: account-info
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}auth/account-info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
