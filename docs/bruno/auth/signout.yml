info:
  name: signout
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}auth/sign-out"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
