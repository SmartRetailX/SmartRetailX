info:
  name: all-microservices-health
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}health/messaging/services"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
