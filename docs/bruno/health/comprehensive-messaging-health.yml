info:
  name: comprehensive-messaging-health
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}health/messaging"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
