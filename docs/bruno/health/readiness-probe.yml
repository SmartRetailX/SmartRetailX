info:
  name: readiness-probe
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}health/ready"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
