info:
  name: rabbitMQ-connection-check
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}health/messaging/rabbitmq"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
