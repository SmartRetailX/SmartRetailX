info:
  name: get-products
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}core/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
