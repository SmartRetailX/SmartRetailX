import { ShortcutDefinition } from '@/utils/keyboard-shortcuts';

export const OPEN_CONTRACTS_SHORTCUT: ShortcutDefinition = {
  key: 'a',
  primary: true,
  shift: true,
};

export const ADD_ROW_SHORTCUT: ShortcutDefinition = {
  key: 'Enter',
};

export const NEXT_CONTRACT_AMOUNT_SHORTCUT: ShortcutDefinition = {
  key: 'Enter',
  primary: true,
};
