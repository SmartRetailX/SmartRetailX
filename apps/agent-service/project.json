{
  "name": "agent-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/agent-service",
  "targets": {
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/python -m pip install -r requirements.txt",
        "cwd": "apps/agent-service"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bash -lc '.venv/bin/python -u -m uvicorn main:app --reload --reload-dir agent_service --reload-include \"*.py\" --host \"${AGENT_HTTP_HOST:-0.0.0.0}\" --port \"${AGENT_HTTP_PORT:-8010}\" --log-level info'",
        "cwd": "apps/agent-service"
      }
    }
  },
  "tags": ["type:app", "lang:python", "scope:assistant"]
}
